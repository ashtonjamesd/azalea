#include "math.h"
#include<stdio.h>

int _pivot_exp(void *args[]) {
    int total = 1;

    // printf("  %d\n", *(int *)args[0]);
    // printf("  %d", *(int *)args[1]);
    return 1;
    // for (int i = 0; i < exp; i++) {
    //     total *= n;
    // }

    // printf("%d", total);
    // return total;
}
